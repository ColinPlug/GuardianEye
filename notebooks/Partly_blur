import cv2

img = cv2.imread("./test.jpg")

roi = img[100:900, 100:600]

blurred_roi = cv2.GaussianBlur(roi, (151, 151), 0)

img[100:900, 100:600] = blurred_roi

cv2.imwrite('test_blurred.jpg', img)

cv2.imshow('Origineel', img)

cv2.waitKey(0)
cv2.destroyAllWindows()
